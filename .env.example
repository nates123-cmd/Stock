# Stock — environment (spec §14.2 open question: API key handling)
#
# Single-user personal build: paste your Anthropic API key here and copy this
# file to `.env`. EXPO_PUBLIC_* is inlined into the client bundle, so this is
# fine for a personal install but must NOT ship in a distributed binary.
EXPO_PUBLIC_ANTHROPIC_API_KEY=
