# Local `supabase functions serve` only. Copy to supabase/functions/.env
# (gitignored). In the cloud these are set via `supabase secrets set` instead.
ANTHROPIC_API_KEY=
# Optional shared secret. If set, the client must send it in the
# x-stock-proxy-secret header. See the function header for the caveat.
STOCK_PROXY_SECRET=
