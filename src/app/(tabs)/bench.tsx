import { PillarPlaceholder } from '@/components';

/** Bench — conversions and substitutions (spec §9). */
export default function BenchScreen() {
  return (
    <PillarPlaceholder
      pillar="Bench"
      icon="bench"
      tagline="The workbench handles conversions and substitutes — cups → grams, ingredient → swap, with baker's % for doughs."
      specRef="§9"
    />
  );
}
